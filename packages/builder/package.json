{
  "name": "@ollierelph/openapi-builder",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tscheck": "tsc --noEmit",
    "test:unit": "vitest --run",
    "build": "pnpm tsc -p tsconfig.build.json",
    "format": "prettier --write ./{src,test}",
    "publish-package": "exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ollierelph/openapi-parser": "workspace:*",
    "@ollierelph/openapi-visitor": "workspace:*"
  },
  "devDependencies": {
    "@effect/data": "^0.14.0",
    "@effect/schema": "^0.19.3",
    "@types/node": "^18.16.1",
    "ts-pattern": "^5.0.1"
  },
  "peerDependencies": {
    "@effect/data": "^0.14.0",
    "@effect/schema": "^0.19.3"
  }
}
